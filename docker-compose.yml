services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: trading_site
    volumes:
      - trading_site_postgres:/var/lib/postgresql/data

volumes:
  trading_site_postgres:

